<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ProjectCreateMapper">
	<select id="getCurrentProject" resultType="com.gamepound.app.project.vo.ProjectVo">
		SELECT * FROM PROJECT WHERE STATUS_NO = 1 AND MEMBER_NO = #{no}
	</select>
</mapper>